# 2026-08-27

- SwiftUI: `scrollBounceBehavior(.basedOnSize)` is nice for iOS 16+ to avoid bouncing when content fits.
- Flutter: use `const` constructors where possible to reduce rebuild cost.
- Xcode 16 simulator sometimes needs `xcrun simctl shutdown all` before booting multiple devices.
